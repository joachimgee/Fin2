"""Trading strategies. Depend on: data + shared (NEVER execution, NEVER alpaca-py).

One strategy = one file extending AbstractStrategy (Open/Closed: adding a
strategy never modifies base.py or the engines).
"""
